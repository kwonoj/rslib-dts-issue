{
  "name": "repro-rslib-dts",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "repro": "pnpm --filter pkg-b run build",
    "runtsc": "pnpm --filter pkg-b run tsc:build"
  },
  "devDependencies": {
    "@rslib/core": "^0.17.2",
    "typescript": "^5.7.2"
  }
}
